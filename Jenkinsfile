pipeline{
    agent any
    
    stages{
        
        stage("Build "){
        steps{
        echo("Build the project")
        }
        }
        
        
          stage("Deploy proj"){
               steps{
        echo("Deploy the project")
          }
          }
          
           stage("Deploy to qa "){
               steps{
        echo("Deploy the project to qa")
          }
          }
       stage("Deploy to prod "){
               steps{
        echo("Deploy the project to prod")
          }
          }
    }
}